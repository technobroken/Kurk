extends Area2D
class_name Collectible

enum EType{Gun}
export(EType)var Type
export(PackedScene)var Prefab:PackedScene

func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass

